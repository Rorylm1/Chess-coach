# Chess Coach — environment variables
# Copy to .env.local and fill in. Server-side only; never expose to the client.

# Anthropic API key for the server-side coaching route (used from M3 onward).
ANTHROPIC_API_KEY=
